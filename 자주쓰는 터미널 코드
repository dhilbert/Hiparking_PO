가상 환경 만들기
C:\Users\82107\venvs\hip_gauss_yoon\Scripts\Activate.ps1

source gaussyoon/bin/activate



가상 환경 끄기
deactivate


pip install -r requirements.txt




git status            # 변경 사항 확인
git add .             # 전체 변경사항 스테이징
git commit -m "업데이트 내용"   # 커밋 메시지
git push              # 원격(main 또는 origin의 기본 브랜치)으로 푸시


# 🔽 로컬 브랜치 최신으로 갱신 (기본)
git pull origin main

# or 현재 체크아웃된 브랜치 기준으로 pull
git pull

# 🔻 만약 충돌나면 상태 확인
git status

# 🔧 충돌 해결 후
git add .
git commit -m "merge conflict resolved"
git pull
